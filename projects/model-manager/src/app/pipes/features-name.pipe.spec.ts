import { FeaturesNamePipe } from './features-name.pipe';

describe('FeaturesNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FeaturesNamePipe();
    expect(pipe).toBeTruthy();
  });
});
