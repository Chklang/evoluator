import { ResourcesNamePipe } from './resources-name.pipe';

describe('ResourcesNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ResourcesNamePipe();
    expect(pipe).toBeTruthy();
  });
});
