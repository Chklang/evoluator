import { RemoveUsedFromFormgroupPipe } from './remove-used-from-fromgroup.pipe';

describe('RemoveUsedFromFormgroupPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveUsedFromFormgroupPipe();
    expect(pipe).toBeTruthy();
  });
});
